#include<stdio.h>

int main(){
    int  i,n,sum=0;
    printf("Enter the value for N: \n");
    scanf("%d",&n);
    i=1;
    while(i<=n)
    {
       sum = sum + i;
       i++;
    }
    printf("The sum of odd numbers up to %d is %d\n", n, sum);
    return 0;
}